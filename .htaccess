<IfModule mod_rewrite.c>
    RewriteEngine On

    # 1. Handle the Demo Subfolder
    RewriteRule ^demo/(.*)$ demo/public/$1 [L]

    # 2. Handle the Main Site
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteCond %{REQUEST_URI} !^/demo/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
